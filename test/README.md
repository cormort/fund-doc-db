# 測試素材

以瀏覽器 console 手動驗證解析路徑（`python3 -m http.server` 後開啟 index.html）：

```js
const p = await import('./js/parsers.js');
const f = new File([await (await fetch('./test/fixtures/heading-styles.docx')).blob()], 'of115ds-16-交作.docx');
const out = []; await p.parseDocx(f, out, '作業基金', '115'); console.log(out[0]);
```

- `heading-styles.docx`：使用 Word 標題 1／2 樣式 → 應走樣式路徑，無警告。
- `plain-text.docx`：無標題樣式 → 應退回文字規則，並回報「未使用 Word 標題樣式」與「基金名稱由檔名推定」。

## PDF 版面處理

`two-column.pdf`（自動產生的最小 PDF，3 頁雙欄，含重複頁首與頁碼）：

```js
const p = await import('./js/parsers.js');
const f = new File([await (await fetch('./test/fixtures/two-column.pdf')).blob()], 'two-column.pdf');
const out = []; await p.parsePdf(f, out, '作業基金', '115'); console.log(out[0]);
```

預期：警告列出「雙欄版面已重排」與「已移除重複頁首／頁尾」，內容為左欄 0–9 全部排在右欄 0–9 之前。

`budget-table.pdf`（3 頁、一欄科目兩欄金額）換成同樣的呼叫，預期改為警告「疑似表格，維持逐列讀取」，
且內容保持 `Item0 10000 20000` 的逐列順序，不被拆成先讀完整欄。

單元層級可直接餵假的 text item（`{str, width, height, transform}`）給 `extractPageLines(items, pageWidth)`
與 `stripRunningLines(pages, warnings)`，驗證三種版面：真雙欄要判 2、單欄整行與「單行被切成兩片段」都要判 1。

表格判定（`looksLikeTable(items, pageWidth)`）三條件任一成立即視為表格：
三個以上欄位起點在 60% 的列上對齊、任一側文字平均填滿度低於欄寬 60%、或右側 60% 的列為純數字。

## 實際文件驗證（2026-09-21）

以 116 年度分析說明 PDF 10 份（國發、營建、平均地權、生服、眷改、軍教、大學、臺大醫、成大醫、陽交大醫）實跑，
文件未進版控（屬未公開預算資料，僅在本機以 `python3 -m http.server` 測試）。結果：

- 基金名稱 10/10 由文件內文取得（先前 8 份誤用檔名，原因是標題以字距排版使「基 金」無法比對）。
- 階層 8–9 段／份，三個小標題（業務收支、餘絀撥補、現金流量）份份齊全。
- 僅 1 份出現警告（生服第 4 頁疑似表格），其餘無警告。
- 內容涵蓋率抽查 80–91%；缺口主要來自「二、最近 5 年主要營運項目」表格依既有設計以佔位字串取代。

## 表格解析驗證（2026-09-21）

### 全量：116 年度分析說明 PDF 59 份

- 基金名稱 **59/59** 取自文件內文。
- 「二、最近 5 年主要營運／業務項目」表格 **59/59 解析成功**，共 211 個資料列。
- **54 份零警告**；5 份標記待人工核對：掃描影像頁 1、疑似表格頁 1、數值格式異常 3（說明文字混入表格、兩列被併成一列）、標題與表格同行 1。

過程中依實際資料修掉的系統性問題：標題列因 1–3pt 座標抖動被拆成兩行、欄位分群在多種間距下都湊得出正確欄數
而選到錯的一組（改以年度列為唯一錨點）、續表重複標題列被當成資料、表格下方說明文字被硬塞進欄位。

### 初期抽樣：10 份：「二、最近5年主要營運項目」表格 **10/10 解析成功**，共 27 個資料列，
數值格式檢查 0 例異常（檢查式 `^(-|數字千分位|(括號負數))$`）。內容涵蓋率由 80–91% 提升到 95–97%
（剩餘缺口為表格標題列等版面文字）。合成 fixture 與 DOCX 兩條路徑無迴歸。

### 全量：116 年度 DOCX 54 份

以 Word 原始儲存格解析表格：**54/54 成功**，共 178 個資料列，基金名稱 54/54 取自文件，零數值格式警告。
54 份文件**全部都沒有套用 Word 標題樣式**，一律走文字規則路徑（樣式路徑僅在有套樣式的文件生效）。
註：另有 35 份 .doc（舊版二進位格式）本工具不支援，需先另存為 .docx。
